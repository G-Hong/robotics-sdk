[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "nexodim"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "opencv-python",
]

[tool.setuptools.packages.find]
include = ["nexodim*"]